version: '3.8'

services:
  proof-server:
    container_name: 'reap-proof-server'
    image: "midnightntwrk/proof-server:7.0.0"
    ports:
      - "6300:6300"
    environment:
      RUST_BACKTRACE: "full"